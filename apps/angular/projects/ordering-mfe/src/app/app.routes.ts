import { Routes } from '@angular/router';
import { CartPageComponent } from './ordering/cart-page/cart-page.component';

export const routes: Routes = [
  { path: '', component: CartPageComponent }
];
